node_modules/
*.log
.DS_Store

# The first skills/ script to run re-execs itself into one shared venv (see
# README's "Is setup automated?") and writes a torch-version pin next to it —
# neither belongs in version control.
skills/.venv/
skills/.uv-constraints.txt
skills/**/__pycache__/
